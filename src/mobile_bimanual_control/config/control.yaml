command_bridge:
  ros__parameters:
    command_rate_hz: 50.0
    joint_state_timeout_sec: 0.1

    # Limits, intentionally conservative.
    max_velocity_rad_s: 0.5
    max_target_step_rad: 0.25
    max_tracking_error_rad: 0.25
